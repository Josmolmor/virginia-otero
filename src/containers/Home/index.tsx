import { SectionTitle } from '$/styles/mixins';

import { Container } from './styles';

const Home = () => (
  <Container>
    <SectionTitle>Hola!</SectionTitle>
  </Container>
);

export default Home;
